# vigilant-hop
This will detect for strange vigilant hops that appear in your life when you use traceroute 

- [ ] Use different mechanisms receive input: piped, files, online etc
- [ ] Use different mechanisms to detect the change: simples diff, incremental diff, odd/even changes, geographic changes, time changes, ttls changes
- [ ] blacklist/whitelist detection
- [ ] specific field change
- [ ] Formula applyed to fields: ie. this metric corresponds to other mapping specified ? Ie. ttl (delta) X  correspond to mapping X

